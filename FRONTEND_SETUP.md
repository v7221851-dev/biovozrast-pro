# 🚀 React Frontend - Быстрый старт

## ✅ Что уже сделано:

1. ✅ Создан React проект с Vite + TypeScript + Tailwind CSS
2. ✅ Перенесена логика расчетов на JavaScript
3. ✅ Созданы все компоненты UI в стиле Tilda Quiz
4. ✅ Реализована навигация между шагами
5. ✅ Добавлен спидометр результатов
6. ✅ Создана форма отзывов

## 🎯 Запуск проекта:

```bash
cd frontend
npm install
npm run dev
```

Приложение откроется на `http://localhost:5173`

## 📁 Структура:

```
frontend/
├── src/
│   ├── components/
│   │   ├── steps/          # 4 шага теста
│   │   ├── ProgressBar.tsx
│   │   ├── NavigationButtons.tsx
│   │   └── ResultsGauge.tsx
│   ├── utils/
│   │   ├── calculations.ts  # Формулы расчетов
│   │   └── resultDescription.ts
│   ├── types/               # TypeScript типы
│   └── App.tsx              # Главный компонент
```

## 🎨 Особенности:

- **Адаптивный дизайн** - работает на всех экранах
- **Цвета из Tilda** - `#3B46EE` основной цвет
- **Плавные переходы** - между шагами
- **Быстрая загрузка** - оптимизированный бандл
- **TypeScript** - типобезопасность

## 📦 Деплой:

### Vercel (самый простой):
```bash
npm install -g vercel
cd frontend
vercel
```

### Или через GitHub:
1. Запушьте код на GitHub
2. Подключите репозиторий к Vercel
3. Автоматический деплой при каждом push

## 🔗 Интеграция с Tilda:

После деплоя добавьте iframe в Tilda:

```html
<iframe 
  src="https://your-app.vercel.app" 
  width="100%" 
  height="100vh" 
  frameborder="0"
></iframe>
```

## ✨ Следующие шаги:

1. Протестируйте локально: `npm run dev`
2. Проверьте все шаги теста
3. Задеплойте на Vercel
4. Интегрируйте с Tilda через iframe

## 🐛 Если что-то не работает:

1. Убедитесь, что установлены все зависимости: `npm install`
2. Проверьте версию Node.js (рекомендуется 18+)
3. Очистите кэш: `rm -rf node_modules && npm install`
