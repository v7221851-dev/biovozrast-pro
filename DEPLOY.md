# Инструкция по деплою и интеграции с Tilda

## 🚀 Деплой на Streamlit Cloud (бесплатно)

### Шаг 1: Подготовка проекта

1. Убедитесь, что все файлы готовы:
   - `app.py` - основной файл приложения
   - `requirements.txt` - зависимости
   - `fonts/DejaVuSans.ttf` - шрифт для PDF
   - `.streamlit/config.toml` - конфигурация

2. Проверьте `requirements.txt`:
```
streamlit
pandas
plotly
fpdf2
```

### Шаг 2: Загрузка на GitHub

1. Создайте репозиторий на GitHub (если еще нет)
2. Загрузите код:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
git push -u origin main
```

**Важно:** Убедитесь, что `.gitignore` не исключает `.streamlit/config.toml`

### Шаг 3: Деплой на Streamlit Cloud

1. Перейдите на https://share.streamlit.io/
2. Войдите через GitHub
3. Нажмите "New app"
4. Выберите ваш репозиторий
5. Укажите путь к файлу: `app.py`
6. Нажмите "Deploy"

После деплоя вы получите ссылку вида: `https://ваш-проект.streamlit.app`

## 🔗 Интеграция с Tilda

### Вариант 1: Прямая ссылка (рекомендуется)

На кнопке "Начать тест" в Tilda установите ссылку:
```
https://ваш-проект.streamlit.app
```

**Плюсы:**
- Простая настройка
- Полная функциональность
- Адаптивный дизайн

### Вариант 2: Встраивание через iframe

1. В редакторе Tilda добавьте блок **"Т123. HTML-код"**
2. Вставьте следующий код:

```html
<iframe 
    src="https://ваш-проект.streamlit.app/?embed=true" 
    style="width:100%; height:900px; border:none; display:block;" 
    allow="geolocation; microphone; camera"
    title="Тест на биологический возраст">
</iframe>
```

**Параметры:**
- `?embed=true` - убирает верхнюю панель Streamlit
- `height:900px` - настройте под ваш контент
- `width:100%` - растягивается на всю ширину

### Вариант 3: Открытие в новом окне (popup)

В Tilda можно настроить кнопку, которая открывает тест в модальном окне:

```html
<a href="https://ваш-проект.streamlit.app" 
   target="_blank" 
   style="display:inline-block; padding:15px 30px; background:#636EFA; color:white; text-decoration:none; border-radius:5px;">
   Начать тест
</a>
```

## 📱 Адаптивность

Streamlit автоматически адаптируется под мобильные устройства. Для iframe на мобильных может потребоваться дополнительная настройка:

```html
<style>
@media (max-width: 768px) {
    iframe {
        height: 1200px !important;
    }
}
</style>
```

## 🔒 Безопасность

Настройки в `.streamlit/config.toml`:
- `enableCORS = false` - для работы в iframe
- `enableXsrfProtection = false` - для встраивания
- `headless = true` - для серверного режима

## ✅ Проверка после деплоя

1. Откройте ссылку Streamlit Cloud
2. Проверьте все функции теста
3. Убедитесь, что PDF генерируется корректно
4. Протестируйте на мобильных устройствах

## 🆘 Решение проблем

**Проблема:** Приложение не загружается в iframe
- Решение: Проверьте настройки CORS в `config.toml`

**Проблема:** Шрифты не работают в PDF
- Решение: Убедитесь, что `fonts/DejaVuSans.ttf` загружен в репозиторий

**Проблема:** Ошибки при деплое
- Решение: Проверьте `requirements.txt` и логи в Streamlit Cloud

## 📞 Поддержка

Если возникли проблемы, проверьте:
- Логи деплоя в Streamlit Cloud Dashboard
- Консоль браузера (F12) для ошибок JavaScript
- Настройки безопасности браузера
