# Интеграция React-приложения в Tilda

## 📋 Обзор решения

Для встраивания React-приложения теста на биологический возраст в Tilda используется технология **iframe**. Это стандартный и надежный способ интеграции внешних приложений.

## 🎯 Варианты деплоя

### Вариант 1: Vercel (Рекомендуется) ⭐

**Преимущества:**
- Бесплатный хостинг
- Автоматический деплой из GitHub
- HTTPS по умолчанию
- Быстрая загрузка
- Простая настройка

**Шаги:**
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Настройки деплоя:
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. После деплоя получите URL вида: `https://your-project.vercel.app`

### Вариант 2: Netlify

**Преимущества:**
- Бесплатный хостинг
- Простой drag-and-drop деплой
- Автоматический HTTPS

**Шаги:**
1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Перетащите папку `frontend/dist` в Netlify
3. Или подключите GitHub репозиторий
4. Получите URL вида: `https://your-project.netlify.app`

### Вариант 3: GitHub Pages

**Преимущества:**
- Бесплатно
- Интеграция с GitHub

**Шаги:**
1. Соберите проект: `cd frontend && npm run build`
2. Создайте репозиторий `gh-pages` или используйте GitHub Actions
3. Настройте GitHub Pages в настройках репозитория
4. URL будет: `https://username.github.io/repo-name`

## 🔧 Настройка Vite для iframe

Перед деплоем нужно настроить `vite.config.ts` для правильной работы в iframe:

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/', // Для Vercel/Netlify оставьте '/'
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  },
})
```

## 📝 Код для вставки в Tilda

### Способ 1: HTML-блок Tilda (Рекомендуется)

1. В редакторе Tilda добавьте блок **"Т123. HTML-код"**
2. Вставьте следующий код:

```html
<style>
    .bioage-test-container {
        width: 100%;
        min-height: 900px;
        border: none;
        display: block;
        margin: 0 auto;
        background: transparent;
    }

    /* Адаптивность для разных экранов */
    @media (max-width: 768px) {
        .bioage-test-container {
            min-height: 1200px;
        }
    }

    @media (max-width: 480px) {
        .bioage-test-container {
            min-height: 1400px;
        }
    }

    /* Скрытие скроллбара iframe (опционально) */
    .bioage-test-container::-webkit-scrollbar {
        display: none;
    }
</style>

<iframe 
    src="https://ВАШ-URL.vercel.app" 
    class="bioage-test-container"
    allow="geolocation; microphone; camera" 
    title="Тест на биологический возраст"
    scrolling="auto"
    frameborder="0"
    loading="lazy">
    <p>Ваш браузер не поддерживает iframe. 
    <a href="https://ВАШ-URL.vercel.app" target="_blank">Открыть тест в новом окне</a></p>
</iframe>
```

**⚠️ Важно:** Замените `https://ВАШ-URL.vercel.app` на реальный URL вашего деплоя.

### Способ 2: Zero Block Tilda

Если используете Zero Block:
1. Добавьте Zero Block
2. Вставьте тот же HTML код выше
3. Настройте отступы блока в редакторе Tilda

### Способ 3: Кнопка-триггер (Альтернатива)

Если хотите открывать тест в модальном окне или новой вкладке:

```html
<style>
    .bioage-test-button {
        display: inline-block;
        padding: 16px 40px;
        background: #3B46EE;
        color: white;
        text-decoration: none;
        border-radius: 30px;
        font-size: 18px;
        font-weight: 600;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
    }
    
    .bioage-test-button:hover {
        background: #2B36CC;
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(59, 70, 238, 0.3);
    }
</style>

<a href="https://ВАШ-URL.vercel.app" 
   target="_blank" 
   rel="noopener noreferrer"
   class="bioage-test-button">
   🧬 Пройти тест на биологический возраст
</a>
```

## 🎨 Настройка стилей для интеграции

### Скрытие элементов Tilda вокруг iframe

Если нужно скрыть элементы Tilda (шапка, футер) на странице с тестом:

1. В настройках страницы Tilda отключите шапку и футер
2. Или используйте CSS в HTML-блоке:

```html
<style>
    /* Скрыть шапку Tilda на этой странице */
    .t-header {
        display: none !important;
    }
    
    /* Скрыть футер Tilda на этой странице */
    .t-footer {
        display: none !important;
    }
</style>
```

### Адаптивная высота iframe

Для автоматической подстройки высоты iframe под контент можно использовать JavaScript:

```html
<script>
    window.addEventListener('message', function(event) {
        // Получаем высоту от React приложения
        if (event.data && event.data.type === 'resize') {
            const iframe = document.querySelector('.bioage-test-container');
            if (iframe) {
                iframe.style.height = event.data.height + 'px';
            }
        }
    });
</script>
```

И в React приложении добавьте отправку высоты:

```typescript
useEffect(() => {
    const sendHeight = () => {
        const height = document.documentElement.scrollHeight;
        window.parent.postMessage({ type: 'resize', height }, '*');
    };
    
    sendHeight();
    window.addEventListener('resize', sendHeight);
    return () => window.removeEventListener('resize', sendHeight);
}, []);
```

## 🔒 Безопасность и производительность

### Content Security Policy (CSP)

Если Tilda блокирует iframe, добавьте в настройки Tilda или в HTML:

```html
<meta http-equiv="Content-Security-Policy" 
      content="frame-src https://ВАШ-URL.vercel.app;">
```

### Оптимизация загрузки

1. Используйте `loading="lazy"` для iframe (добавлено в код выше)
2. Минифицируйте React приложение: `npm run build`
3. Включите gzip сжатие на хостинге (Vercel/Netlify делают это автоматически)

## 📱 Тестирование

После интеграции проверьте:

1. ✅ Тест открывается в iframe
2. ✅ Все шаги работают корректно
3. ✅ Адаптивность на мобильных устройствах
4. ✅ Кнопки и формы функционируют
5. ✅ Скачивание PDF отчета работает
6. ✅ Отправка отзывов в Google Sheets работает

## 🐛 Решение проблем

### Проблема: Iframe не загружается

**Решение:**
- Проверьте, что URL правильный и доступен
- Убедитесь, что сайт использует HTTPS
- Проверьте настройки CSP в Tilda

### Проблема: Стили не применяются

**Решение:**
- Убедитесь, что в `vite.config.ts` указан правильный `base`
- Проверьте, что все CSS файлы загружаются
- Очистите кеш браузера

### Проблема: Ошибки CORS

**Решение:**
- Убедитесь, что API (Google Sheets) настроены правильно
- Проверьте настройки CORS на сервере

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте Network вкладку для загрузки ресурсов
3. Убедитесь, что все зависимости установлены

## 🚀 Быстрый старт

1. **Деплой:**
   ```bash
   cd frontend
   npm run build
   # Загрузите dist/ на Vercel/Netlify
   ```

2. **Получите URL:** `https://your-project.vercel.app`

3. **Вставьте в Tilda:** Используйте код из раздела "Код для вставки в Tilda"

4. **Готово!** Тест работает на вашем сайте Tilda
